import { white, blackGrey, grey, darkGrey } from "../color";

import { ITheme } from "./types";

const theme: ITheme = {
  colors: {
    textPrimary: white,
    textSecondary: blackGrey,
    backgroundPrimary: grey,
    backgroundSecondary: darkGrey,
  },
};

export default theme;
