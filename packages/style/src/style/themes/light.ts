import { grey, lightGrey, offWhite, white } from "../color";

import { ITheme } from "./types";

const theme: ITheme = {
  colors: {
    textPrimary: grey,
    textSecondary: lightGrey,
    backgroundPrimary: offWhite,
    backgroundSecondary: white,
  },
};

export default theme;
