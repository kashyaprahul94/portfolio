import { white, grey, darkGrey } from "@style/color";

const theme = {
  colors: {
    textPrimary: white,
    backgroundPrimary: grey,
    backgroundSecondary: darkGrey,
  },
};

export default theme;
